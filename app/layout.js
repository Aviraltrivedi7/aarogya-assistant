import './globals.css'

export const metadata = { title: 'AarogyaGPT — Your AI Health Companion', description: 'AI-powered health guidance for everyone.' }

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>
}